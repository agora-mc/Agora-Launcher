// Stub target used by the prefix-collision boundary fixture. It lives in a
// sibling folder (`interactive-app`) outside the interactive scan root and must
// never be importable from Lab/visual code.
export const controller = 'prefix-collision-target';

// Stub app-layer module used by boundary negative fixtures. It lives outside
// `src/features/interactive` and must never be importable from Lab/visual code.
export function someCommand(): void {
  // not a real backend command — fixture stub only
}
